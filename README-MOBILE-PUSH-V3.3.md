# SchoolTask Mobile Push V3.3

Firebase project is intentionally kept on the original `t1-myschedule` project.
The VAPID public key is synchronized to that same Firebase project.

Do not mix a VAPID key from another Firebase project with this config.
