// Web Push / Firebase Cloud Messaging configuration.
// Firebase Console → Project settings → Cloud Messaging → Web configuration
// → Web Push certificates → copy the public VAPID key here.
export const VAPID_KEY = "PASTE_YOUR_FIREBASE_WEB_PUSH_VAPID_PUBLIC_KEY_HERE";
